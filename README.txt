1. The code and two datasets of this submission is commit to my personal github repository. They can be cloned through this command:
         git@github.com:Gerraleslie/SL-hw1.git
2. Some libraries are used: sklearn, matplotlib, numpy, graphviz and lmblearn (for DT) for data processing and plotting.
3. The files of algorithms are jupyter notebook, run the cells in order.
